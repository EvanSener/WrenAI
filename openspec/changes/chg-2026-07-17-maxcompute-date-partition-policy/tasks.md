# 任务

- [x] 增加日期分区元数据解析、校验和 Engine Registry。
- [x] 改造 MaxCompute Connector，避免托管 Model 二次盲补分区。
- [x] 将分区策略编入 Semantic Graph，并支持 Graph `dateRange`。
- [x] 更新 `maxcompute-local` 全部 Model、最新分区 View 和相关 Knowledge。
- [x] 增加 `sp_`、`sb_`、`sd_` 表名片段的初始化推断，并保留显式配置优先级。
- [x] 重建 MDL/Graph/Ontology 产物。
- [x] 完成回归、SQL 复现和真实 MaxCompute 验证。
